/**
 * harness-docs-page — autodocs page renderer with our preferred block order.
 *
 * Source pattern: Fluent UI's FluentDocsPage.
 * Used when a story file has no <Meta of={Stories}> (autodocs path).
 */

import React from 'react';
import {
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  Stories,
} from '@storybook/blocks';

export const HarnessDocsPage = () => (
  <>
    <Title />
    <Subtitle />
    <Description />
    <Primary />
    <Controls />
    <Stories />
  </>
);
